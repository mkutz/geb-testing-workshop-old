language: groovy

addons:
  chrome: stable

jobs:
  include:
    - stage: build
      script: mvn package -DskipTests=true -Dmaven.javadoc.skip=true -B -V
    - stage: class-test
      script: mvn test -B
    - stage: system-test
      script: mvn verify -B -D geb.env=travis
